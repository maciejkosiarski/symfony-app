<div class="btn-group float-right">
    <span class="btn btn-outline-primary disabled">Per page</span>
    <a href="{{ url('exercise_index', {'page': 1, 'limit': 5, 'type': (currentType) ? currentType.id : null }) }}" class="btn btn-outline-primary {% if limit == 5 %} disabled {% endif %}">5</a>
    <a href="{{ url('exercise_index', {'page': 1, 'limit': 10, 'type': (currentType) ? currentType.id : null }) }}" class="btn btn-outline-primary {% if limit == 10 %} disabled {% endif %}">10</a>
    <a href="{{ url('exercise_index', {'page': 1, 'limit': 25, 'type': (currentType) ? currentType.id : null  }) }}" class="btn btn-outline-primary {% if limit == 25 %} disabled {% endif %}">25</a>
</div>
